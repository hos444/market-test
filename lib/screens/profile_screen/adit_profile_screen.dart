import 'package:flutter/material.dart';

class IdatScreen extends StatelessWidget {
  const IdatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('idatn profile')),
      body: Center(child: Text('idatn Screen')),
    );
  }
}
